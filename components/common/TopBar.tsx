import React from 'react';
import { TopBarData } from '@/types/templates.types';

export const TopBar = ({ data }: { data?: TopBarData }) => {
  // TopBar is intentionally left blank for the new design as requested.
  return null;
};
